services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: backstage
      POSTGRES_PASSWORD: backstage_local_dev
      POSTGRES_DB: backstage
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U backstage"]
      interval: 5s
      timeout: 3s
      retries: 5

  backstage:
    build:
      context: .
      dockerfile: packages/backend/Dockerfile
    restart: unless-stopped
    ports:
      - "7007:7007"
    command:
      [
        "node",
        "packages/backend",
        "--config",
        "app-config.yaml",
        "--config",
        "app-config.local-prod.yaml",
      ]
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_USER: backstage
      POSTGRES_PASSWORD: backstage_local_dev
      AUTH_GITHUB_CLIENT_ID: Ov23limeosBqj9yI8QHX
      AUTH_GITHUB_CLIENT_SECRET: 44f2985d14e97fc122c3b7025bc4c2533357908c
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
